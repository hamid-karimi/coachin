# Native `pnpm dev:web` (outside Docker): reach the API through the running stack's
# Caddy (`make up`). The Docker dev container overrides this with http://api:8080.
API_INTERNAL_URL=http://localhost:8080
